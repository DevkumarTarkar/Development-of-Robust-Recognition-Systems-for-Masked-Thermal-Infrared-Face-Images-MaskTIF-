services:
  masktif-backend:
    build: .
    ports:
      - "5001:5001"
    env_file:
      - .env
    volumes:
      # Persist uploads + sqlite database during local dev
      - ./backend/uploads:/app/backend/uploads
      - ./backend/masktif.db:/app/backend/masktif.db

